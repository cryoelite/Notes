- Just like [[Map]] has [[WeakMap]], a [[Set]] has WeakSet.
- Methods:
  ``.add(key,value)``
  ``.has(key)``
  ``.delete(key)``
- Everything is the same as in a [[WeakMap]], that a WeakSet is not [[Iterable]] and only accepts Objects as Keys.
-